export interface LedgerReview {
  UID: string;
  authorId: number;
  authorName: string;
  cid: string;
  created: number;
  title: string;
  subjectId: number;
  studyLevelId: number;
  isPublic: boolean;
}
